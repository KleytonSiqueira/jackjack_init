// Criei para ajudar na manipulação das tasks mas não sei se foi uma boa escolha
export default class Task{
    constructor(title, content, evaluated){
        this.title = title;
        this.content = content;
        this.evaluated = evaluated;
    }
}